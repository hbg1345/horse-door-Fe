# Node.js 20 LTS 이미지 사용
FROM node:20

# 작업 디렉토리 생성
WORKDIR /usr/src/app

# package.json, package-lock.json 복사 및 의존성 설치
COPY package*.json ./
RUN npm install

# 소스 코드 복사
COPY ./src ./src

# (선택) .env 파일 복사 - 실제 배포 시 필요
# COPY .env ./

# 포트 오픈 (기본값 8080, 환경 변수로 변경 가능)
EXPOSE 8080

# 서버 실행
CMD ["npm", "start"]
